interface:
  display_name: "Code Checkpoint"
  short_description: "Create frequent reversible git checkpoints."
  default_prompt: "Use $Code_Checkpoint to checkpoint work after meaningful milestones and before risky git operations without relying on local aliases."
