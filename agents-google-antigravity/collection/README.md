# Google Antigravity Collection

Plugin source root for Gymnasium's Google Antigravity material.

The installed `agy` CLI validates individual plugin directories with
`agy plugin validate <plugin-dir>`. Until Google documents a marketplace or
collection manifest format for Antigravity, keep this collection as a source
tree rather than inventing a manifest.
